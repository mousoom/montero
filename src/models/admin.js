class users {
  constructor(id, username, email, userType, department) {
    this.id = id;
    this.username = username;
    this.email = email;
    this.userType = userType;
    this.department = department;
  }
}

export default users;
